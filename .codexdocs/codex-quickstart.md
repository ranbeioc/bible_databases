# Codex Quickstart

Read docs first. Then:
1. Verify current schema and repository state
2. Preserve accepted architecture decisions
3. Propose next 3 safest implementation steps
4. Only then start coding

Core reminders:
- verse_id = semantic key
- ref = display key
- Bible reading = static JSON + CDN
- PostgreSQL + pgvector + PostGIS
- Local embedding preferred
